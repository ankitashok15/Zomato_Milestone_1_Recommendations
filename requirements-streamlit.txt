# Local optional: pip install -r requirements-streamlit.txt
# Streamlit Cloud: default is repo-root requirements.txt (includes python-dotenv).
# You can still point Advanced → packages file here if you prefer a separate file.
streamlit>=1.28,<2.0
pandas>=2.2,<3.1
pydantic>=2.7,<3.0
httpx>=0.28,<1.0
python-dotenv>=1.0,<2.0
